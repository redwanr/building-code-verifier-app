# Copy to .streamlit/secrets.toml (gitignored). On Streamlit Community Cloud,
# paste these into the app's Secrets settings instead.
APP_PASSWORD = "choose-a-shared-password"
ANTHROPIC_API_KEY = "sk-ant-..."
GEMINI_API_KEY = "..."
